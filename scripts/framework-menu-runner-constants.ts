export const DEFAULT_ACTIONLINT_BIN =
  process.env.PUMUKI_ACTIONLINT_BIN?.trim() || 'actionlint';
export const DEFAULT_CONSUMER_REPO_PATH =
  process.env.PUMUKI_CONSUMER_REPO_PATH?.trim() || '/path/to/consumer-repo';
