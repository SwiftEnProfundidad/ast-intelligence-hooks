describe('utils', () => {
    describe('token-manager', () => {
        it('should export module', () => {
            const mod = require('../token-manager');
            expect(mod).toBeDefined();
        });
    });
});
