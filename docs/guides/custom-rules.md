# Custom AST Rules

Cómo definir reglas AST personalizadas sobre la infraestructura existente.

## Conceptos clave

- Analizadores por plataforma (`ast-backend.js`, `ast-frontend.js`, etc.).
- Uso de `ts-morph` para reglas TypeScript.
- Reutilización de `Finding` y `AuditResult` como entidades de dominio.

> TODO: Añadir pasos detallados para:
>
> - Añadir una nueva regla.
> - Probar la regla.
> - Documentarla en `docs/rules/*.md`.
