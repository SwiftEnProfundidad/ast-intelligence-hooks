name: Pumuki Deterministic Tests

on:
  pull_request:
  push:

jobs:
  deterministic-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node LTS
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run deterministic suite
        run: npm run test:deterministic
